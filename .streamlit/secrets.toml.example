# Streamlit Cloud Secrets — example file
# Go to: app dashboard → Settings → Secrets
# Paste the line below (without the # comment) with your actual key:

GROQ_API_KEY = "gsk_your_actual_groq_api_key_here"
